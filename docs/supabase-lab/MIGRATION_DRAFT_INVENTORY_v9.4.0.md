# Migration Draft Inventory v9.4.0

## Purpose

This inventory lists the SQL draft files that may later become a reviewed Supabase Lab migration bundle.

## SQL Drafts

| File | Role |
|---|---|
| `docs/supabase/sql-drafts/v5.3.0_membership_tables_draft.sql` | membership and workspace foundation |
| `docs/supabase/sql-drafts/v5.4.0_usage_quota_tables_draft.sql` | quota policy and usage tracking |
| `docs/supabase/sql-drafts/v5.5.0_audit_approval_tables_draft.sql` | audit, approval, export, manual review |
| `docs/supabase/sql-drafts/v5.6.0_rls_policy_draft.sql` | RLS draft and role protection direction |

## Current Status

These are draft files.

They are not applied.

They require review before use.

## Lab Boundary

If applied later, they must target only a Supabase lab project or a clearly separated namespace.
