# v12.3.0 — Finance Proof Page Skeleton

Adds public proof page:

```text
/finance-proof
```

Static, local status only.

No database connection.

No mutation.

The page renders the canonical line through the local proof status model:

```text
status.canonicalLine
```
