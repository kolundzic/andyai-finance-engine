# Working Core v2.0.0

This layer turns the canon into a local runnable proof.

## Flow

Input JSON → loader → pricing calculation → summary → scenarios → audit trail → markdown export pack.

## Truth rule

AI can build the model fast. But the human must verify that the model tells the truth.
