# Schema Overview

The schema is intentionally conservative.

It separates:

- project identity
- business input
- model run
- audit event
- human approval decision
- export package

This prevents AI-generated numbers from being mixed with human-approved decisions.
