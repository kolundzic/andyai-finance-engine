# TAPFORGE Server-Side Runtime Boundary Record v10.6.0

## Purpose

This document records the server-side boundary before any future executor.

## Gate

The system may prepare server-side read-only execution later.

It may not expose privileged secrets to browser code.

## Rule

No server boundary, no executor.
