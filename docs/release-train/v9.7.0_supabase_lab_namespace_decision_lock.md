# v9.7.0 — Supabase Lab Namespace Decision Lock

## Summary

v9.7.0 locks the namespace decision for the shared Supabase Free lab.

## Decision

Use prefix-based isolation for the first Finance Engine lab apply candidate:

- `fe_` for Finance Engine
- `tf_` for TAPFORGE
- `audit_` for shared audit
- `human_attention_` remains protected and untouched

## Safety

No migration is applied.

No schema is created.

No table is created.

No data is written.

No Vercel environment variable is changed.

No checkout, webhook, billing provider, subscription enforcement, or paid runtime behavior is introduced.
