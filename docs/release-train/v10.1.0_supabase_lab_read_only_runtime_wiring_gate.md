# v10.1.0 — Supabase Lab Read-Only Runtime Wiring Gate

## Summary

v10.1.0 defines the read-only runtime wiring gate after the v10.0.0 first SQL apply and v10.0.1 post-apply seal.

## Scope

- Adds read-only runtime wiring gate
- Adds read-only access contract
- Adds table allowlist
- Adds TAPFORGE runtime gate record
- Adds JSON manifest
- Adds verifier

## Safety

No SQL is applied by this release.

No schema is created by this release.

No table is created by this release.

No data is written by this release.

No Vercel environment variable is changed.

No checkout, webhook, billing provider, subscription enforcement, or paid runtime behavior is introduced.
