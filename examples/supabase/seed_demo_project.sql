-- Optional demo seed for local review.
-- Replace owner_id with a real auth user id before production use.

insert into public.afe_projects (name, description, status)
values ('Demo SaaS Pricing Pack', 'Example project for AndyAI Finance Engine SaaS pricing workflow.', 'draft');

-- Add child rows manually after copying the project id.
